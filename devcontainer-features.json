{
    "features": [
        {
            "id": "azurebicep",
            "name": "Azure Bicep",
            "options": {
                "cli": {
                    "type": "string",
                    "default": "latest",
                    "description": "Specify a version of the Azure Bicep CLI"
                }
            },
            "entrypoint": "/usr/installbicep.sh"
        }
    ]
}